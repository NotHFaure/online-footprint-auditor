[project]
name = "online-footprint-auditor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click",
    "httpx",
    "platformdirs",
]

[project.scripts]
footprint-auditor = "footprint_auditor.cli:cli"

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/footprint_auditor"]

[tool.ruff]
line-length = 100
